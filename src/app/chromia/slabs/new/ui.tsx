/**
 * Re-export of the shared data-entry kit.
 *
 * The intake page was where these shapes were designed; they now live in
 * `components/ui/form` so Operator Entry can use the identical treatment.
 */
export {
  Divider,
  field,
  FieldGrid,
  OutcomePanel,
  SectionCard,
  selectField,
  type Accent,
} from '@/components/chromia/ui/form';
